# HEADER: This file was autogenerated at 2014-01-09 21:00:01 +0100
# HEADER: by puppet.  While it can still be managed manually, it
# HEADER: is definitely not recommended.
define command {
        command_line                   /usr/bin/printf "%b" "***** Nagios *****\n\nNotification Type: $NOTIFICATIONTYPE$\n\nService: $SERVICEDESC$\nHost: $HOSTALIAS$\nAddress: $HOSTADDRESS$\nState: $SERVICESTATE$\n\nDate/Time: $LONGDATETIME$\n\nAdditional Info:\n\n$SERVICEOUTPUT$\n" | /usr/bin/mail -s "** $NOTIFICATIONTYPE$ Service Alert: $HOSTALIAS$/$SERVICEDESC$ is $SERVICESTATE$ **" $CONTACTEMAIL$
        command_name                   notify-service-by-email
}

define command {
        command_line                   /usr/lib/nagios/plugins/check_tcp -H $HOSTADDRESS$ -p $ARG1$
        command_name                   check_tcp
}

define command {
        command_line                   /etc/nagios/scripts/twilio_sms.sh "NOC Message. $NOTIFICATIONTYPE$ Service Alert: $HOSTALIAS$/$SERVICEDESC$ is $SERVICESTATE$ **"
        command_name                   notify-service-by-sms
}

define command {
        command_line                   /usr/lib/nagios/plugins/check_nrpe -H $HOSTADDRESS$ -p $ARG1$
        command_name                   check_nrpe_service
}

define command {
        command_line                   /usr/lib/nagios/plugins/check_nrpe -H $HOSTADDRESS$ -c $ARG1$
        command_name                   check_nrpe_1arg
}

define command {
        command_line                   /etc/nagios/scripts/twilio_voice.sh "Hello. This is your Network Operation Center. $NOTIFICATIONTYPE$ Service Alert: $HOSTALIAS$/$SERVICEDESC$ is $SERVICESTATE$ **"
        command_name                   notify-service-by-voice
}

define command {
        command_line                   /usr/lib/nagios/plugins/check_http -H $HOSTADDRESS$ -I $HOSTADDRESS$
        command_name                   check_http
}

# PNP4NAGIOS Default mode
define command {
       command_name    process-service-perfdata
       command_line    $USER1$/process_perfdata.pl
}

define command {
       command_name    process-host-perfdata
       command_line    $USER1$/process_perfdata.pl -d HOSTPERFDATA
}

# PNP4NAGIOS Bulk mode
define command {
        command_name    process-service-perfdata-file
        command_line /bin/mv /var/spool/pnp4nagios/service-perfdata /var/spool/pnp4nagios/service-perfdata.$TIMET$
}

define command {
        command_name    process-host-perfdata-file
        command_line /bin/mv /var/spool/pnp4nagios/host-perfdata /var/spool/pnp4nagios/host-perfdata.$TIMET$
}

define command {
        command_line                   /usr/lib/nagios/plugins/check_nrpe -H $HOSTADDRESS$ -c $ARG1$ -a $ARG2$
        command_name                   check_nrpe
}

define command {
        command_line                   /usr/bin/printf "%b" "***** Nagios *****\n\nNotification Type: $NOTIFICATIONTYPE$\nHost: $HOSTNAME$\nState: $HOSTSTATE$\nAddress: $HOSTADDRESS$\nInfo: $HOSTOUTPUT$\n\nDate/Time: $LONGDATETIME$\n" | /usr/bin/mail -s "** $NOTIFICATIONTYPE$ Host Alert: $HOSTNAME$ is $HOSTSTATE$ **" $CONTACTEMAIL$
        command_name                   notify-host-by-email
}

define command {
        command_line                   /etc/nagios/scripts/twilio_voice.sh "Hello. This is your Network Operation Center. $NOTIFICATIONTYPE$ Host Alert: $HOSTNAME$ is $HOSTSTATE$ **"
        command_name                   notify-host-by-voice
}

define command {
        command_line                   /usr/lib/nagios/plugins/check_smtp -H $HOSTADDRESS$ -p $ARG1$
        command_name                   check_smtp
}

define command {
        command_line                   /usr/lib/nagios/plugins/check_dns -s $ARG1$ -H $ARG2$
        command_name                   check_dns
}

define command {
        command_line                   /usr/lib/nagios/plugins/check_log -F $ARG1$ -O $ARG2$ -q $ARG3$
        command_name                   check_log
}

define command {
        command_line                   /usr/lib/nagios/plugins/check_ping -H $HOSTADDRESS$ -w $ARG1$ -c $ARG2$
        command_name                   check_ping
}

define command {
        command_line                   /etc/nagios/scripts/twilio_sms.sh "NOC Message. $NOTIFICATIONTYPE$ Host Alert: $HOSTNAME$ is $HOSTSTATE$ **"
        command_name                   notify-host-by-sms
}

define command {
        command_line                   /usr/lib/nagios/plugins/check_ping -H "$HOSTADDRESS$" -w 5000,100% -c 5000,100% -p 1
        command_name                   check-host-alive
}

define command {
	command_line 			/etc/nagios/submit_service_check "$HOSTNAME$" "$SERVICEDESC$" "$SERVICESTATEID$" "$SERVICEOUTPUT$ | $SERVICEPERFDATA$"
	command_name 			submit_service_check
}

define command {
        command_line                    /etc/nagios/submit_host_check "$HOSTNAME$" "$HOSTSTATE$" "$HOSTOUTPUT$"
        command_name                    submit_host_check
}

